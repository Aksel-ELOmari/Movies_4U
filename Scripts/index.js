//! function to fetch favourites Cards
let placeholder = document.querySelector('#SavedElements');
const favourites = [];
let favCards = document.querySelectorAll('.favCard');
favourites.forEach(Card =>{
    console.log(Card);
 })
